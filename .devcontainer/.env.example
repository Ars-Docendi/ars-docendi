# Copy this file to .devcontainer/.env and replace the placeholder.
# .devcontainer/.env is ignored by the repository's existing .gitignore.
ARS_DOCENDI_DEV_DB_PASSWORD=replace-with-the-development-role-password
