## 1. Permission: `asistente.administrar`

- [x] 1.1 Add `AsistenteAdministrar = "asistente.administrar"` to `backend/src/ArsDocendi.Shared/Auth/Permisos.cs`. Verify: existing `Permisos` tests still pass; a new test asserts the constant's exact string value.
- [x] 1.2 Create `database/identity/020_identity_permiso_administrar.sql`, following `014_identity_permiso_ver_consulta.sql`/`019_identity_permiso_leer_historial_ajeno.sql`'s exact shape: idempotent `INSERT ... WHERE NOT EXISTS` seeding `('b2000000-0000-4000-8000-000000000029', 'asistente.administrar', ...)`, plus a grant to `sys_admin` via `identity.rol_permisos`, plus a `DO $administrar$ ... RAISE NOTICE ...$administrar$;` warning block if the grant already differs — with an inline SQL comment recording design.md D13's reasoning for diverging from the "seed to nobody" precedent. Verify: `dotnet test --filter PermisoAdministrarTests` (new suite, written in 1.3) is green.
- [x] 1.3 Write a failing integration test asserting: the permission exists with that exact code, is granted to `sys_admin`, and is **not** granted to any other seeded role by default — mirroring `PermisoLeerHistorialAjenoTests`'s shape but asserting the opposite default (granted to exactly one role, not zero). Then run 1.2 to make it pass. Verify: test green.
- [x] 1.4 Add the matching EF migration wrapper `backend/src/ArsDocendi.Shared/Identity/Migrations/<timestamp>_PermisoAsistenteAdministrar.cs`, mirroring `20260925000000_PermisoAsistenteLeerHistorialAjeno.cs` (`Up` runs the SQL resource, `Down` deletes by code). Verify: `dotnet ef migrations list` (or equivalent) shows it applied cleanly against a fresh database in the integration test harness.
- [x] 1.5 Re-run the `origin/develop` collision check from design.md (context) immediately before this group is considered done: `git fetch origin develop && git diff HEAD origin/develop -- database/identity/ backend/src/ArsDocendi.Shared/Auth/Permisos.cs`. Verify: no new identity migration or `Permisos.cs` constant landed on `develop` that would collide with `020_...`/`b2...029`; if one did, renumber before proceeding.

## 2. Database: budgets, org cap, price table, maintenance flag

- [x] 2.1 Write a failing integration test asserting `database/asistente/006_asistente_administracion.sql` creates, inside the `asistente` schema: `presupuesto_rol` (role code, daily turn quota), `presupuesto_usuario` (actor_id, daily turn quota override, vigente_desde/hasta), `tope_organizacional` (monthly USD cap, vigente_desde), `consumo_organizacional_mensual` (año, mes, costo_estimado_acumulado), `tabla_de_precios` (proveedor, modelo, precio_por_token_entrada, precio_por_token_salida, precio_por_token_cache, version, vigente_desde), and `modo_mantenimiento` (single row: activo, razon, actor_id, actualizado_en). Verify: test passes against a fresh Testcontainers database.
- [x] 2.2 Write the migration file itself, following `002_asistente_registros.sql`'s discipline: `CREATE TABLE IF NOT EXISTS` for every table, no `DEFAULT` on any column a later `ALTER ... ADD COLUMN IF NOT EXISTS` might add, comments explaining each table's "why" inline. Verify: 2.1 passes; `dotnet test backend/ArsDocendi.slnx --filter MigradorAsistenteTests` stays green (migrator still enforces the ADD-COLUMN-only guard on this new file).
- [x] 2.3 Write a failing test asserting `ManifiestoPrivilegiosTests`/`PrivilegiosLecturaTests` still report the `asistente` schema fully denied to `asistente_ro` and `asistente_ro_pii` after 006 lands, with no manifest entry needed for the six new tables. Verify: test passes with zero changes to `manifiesto-privilegios.json`.
- [x] 2.4 Add the six new table/writer class names this change introduces to `ArquitecturaAsistenteTests.PuedenUsarLaCadenaDelDueno`'s allowlist, each with its own one-line documented reason (mirroring how `asistente-historial-conversaciones` added its own). Verify: `dotnet test --filter ArquitecturaAsistenteTests` green.

## 3. Backend: replace `CuotaEnMemoria` with a persistent, turn-counted budget

- [x] 3.1 Write a failing unit test for a new `ICuotaDelActor` contract (turns/day semantics: `Task<bool> HayCupoAsync(Guid actor, CancellationToken ct)`, `Task AnotarAsync(Guid actor, CancellationToken ct)`, `Task<DateTimeOffset?> CupoVuelveAAsync(Guid actor, CancellationToken ct)`, `Task<int> CupoRestanteAsync(Guid actor, CancellationToken ct)`) against an in-memory fake, asserting a user with a role-default quota is blocked after N turns and unblocked after the calendar-day boundary passes (design.md D2: calendar day, UTC). Verify: unit test passes against the fake.
- [x] 3.2 Implement `CuotaPersistente : ICuotaDelActor` (`Infrastructure/`) backed by Postgres: resolves the effective daily quota as per-user override if present and current, else the actor's role default (via `IConsultasIdentity.ObtenerCodigosDeRolesDeSistemaAsync` — the lowest quota among the actor's roles, or a documented "highest wins" rule, decided and written down as a code comment here since design.md left the exact rule to implementation), counts today's turns from `asistente.registro_operativo` (or a dedicated counter table, decided here — prefer counting from `registro_operativo` directly to avoid a second source of truth that could drift). Verify: integration test against a real Postgres instance: quota blocks at N, unblocks the next calendar day.
- [x] 3.3 Delete `CuotaEnMemoria.cs` and its dedicated test file. Verify: solution builds; no remaining reference to the deleted class (`grep -r CuotaEnMemoria backend/src backend/tests` returns nothing).
- [x] 3.4 Write a failing test asserting a per-user override (`presupuesto_usuario`) takes precedence over the role default even when the override is smaller. Implement the precedence rule in 3.2's resolution logic. Verify: test passes for both a larger and a smaller override.
- [x] 3.5 Write a failing test asserting `0` (quota disabled) as a role default behaves exactly like today's `CupoDeLlamadasPorActor <= 0` — quota check always passes, no row is required. Verify: test passes; confirms the "disabled by default" convention design.md's Open Questions recommends carries over.
- [x] 3.6 Write a failing test asserting quota is charged exactly once per turn regardless of outcome (`Respondida`, `NoContestable`, `NecesitaAclaracion`, `ServicioDegradado`, and the `Fallo`/exception path), by wiring `AnotarAsync` into `CapaConversacional`'s existing `finally` block (replacing the `cuota.Anotar` call, same call site, same unconditional guarantee). Verify: test passes for all five outcomes; existing `CapaConversacional`/`CuotaYBreakerTests`-style tests are updated, not duplicated.
- [x] 3.7 Update `OpcionesAsistente`: remove `CupoDeLlamadasPorActor`/`VentanaDeCuotaMinutos` (superseded), keep everything else. Verify: no remaining reference; options-validation tests updated.

## 4. Backend: org-wide monthly cap and price table

- [x] 4.1 Write a failing unit test for a `CalculadoraDeCosto` (or similarly named) service: given a set of `(proveedor, tokens_entrada, tokens_salida, tokens_cache)` rows and a price-table snapshot, returns per-row estimated cost, using the price-table version that was current at each row's `ocurrido_en` timestamp (design.md D1/A1 requirement: historical rows keep the price that was live when they were recorded). Verify: unit test with two price-table versions and rows on both sides of the change-over date.
- [x] 4.2 Write a failing test asserting a row whose `proveedor` has no matching price-table entry is excluded from the cost total and reported separately as "not priced" rather than costed at zero. Implement. Verify: test passes.
- [x] 4.3 Write a failing integration test: the org-wide monthly accumulated cost (`consumo_organizacional_mensual`) updates after each turn using 4.1's calculator, and a turn is blocked with `MotivoSinModelo.TopeOrganizacionalAgotado` once the accumulated cost reaches `tope_organizacional`. Implement the check inside `DisponibilidadDelModeloReal.Consultar` (design.md D3/D4: pre-pipeline check, same choke point as quota). Verify: test passes; a user still under their own daily quota is blocked anyway once the org cap is hit.
- [x] 4.4 Write a failing test asserting the accumulated monthly cost resets to zero at the start of a new calendar month, without any admin action. Verify: test passes (adjust the clock via `TimeProvider`, do not sleep).
- [x] 4.5 Write a failing test asserting `PoliticaDeAbstencion` gains a distinct text for `TopeOrganizacionalAgotado` that never mentions the org's cost or cap value to a non-admin caller (spec: "does not expose the org's cost or cap value to that user"). Verify: test asserts the exact user-facing string contains no numeric cost figure.

## 5. Backend: one in-flight turn per actor (Postgres advisory lock)

- [x] 5.1 Write a failing unit test for a new `CandadoDelTurno` (or similarly named) helper implementing design.md D5: acquires a session-level Postgres advisory lock keyed by a stable hash of the actor's `Guid` via `pg_try_advisory_lock`, exposes `IAsyncDisposable` for guaranteed release, and returns a boolean/throws a typed "already locked" result on failure to acquire. Verify: unit test against a real Postgres connection — two sequential acquisitions from different connections for the same actor: second `pg_try_advisory_lock` call returns false while the first is held; releasing the first lets a third attempt succeed.
- [x] 5.2 Write a failing integration test: a second concurrent turn from the same actor is rejected with `EstadoDelTurno.ServicioDegradado` and `MotivoSinModelo.TurnoConcurrente`'s distinct text, while the first turn completes normally. Wire 5.1 as the very first statement inside `CapaConversacional.ResponderAsync`, released via the same `try/finally` that already wraps quota-charging and history-registration (design.md D5/D4: same exit points, not a new one). Verify: test passes; two different actors submitting concurrently are never blocked by each other (companion assertion in the same or a sibling test).
- [x] 5.3 Write a failing test asserting the lock releases on: a successful turn, an abstained/clarification/degraded turn, an unhandled exception (existing `catch` branch), and the 150 s turn-budget timeout (existing `catch (OperationCanceledException) when (presupuesto.Vencio)` branch) — four scenarios, one test method per scenario or one parameterized test. Verify: after each scenario, the same actor's next turn is accepted immediately (not rejected for concurrency).
- [x] 5.4 Write a failing test asserting a cancelled HTTP request (caller-side cancellation, not a timeout) still releases the lock. Verify: test passes.
- [x] 5.5 Write a failing test asserting two backend-process instances (simulate with two separate `NpgsqlConnection`s against the same database, standing in for two Host instances) observe the same lock for the same actor. Verify: test passes — this is the regression that would fail if the implementation ever regressed to an in-process-only lock.

## 6. Backend: maintenance mode (kill switch)

- [x] 6.1 Write a failing unit test for `IDisponibilidadDelModulo` (design.md D7: `Task<EstadoDeMantenimiento> ConsultarAsync(CancellationToken ct)`, where `EstadoDeMantenimiento` carries `Activo: bool` and `Razon: string?`) against a Postgres-backed `DisponibilidadDelModuloReal` reading the single-row `modo_mantenimiento` table. Verify: unit/integration test passes for both states.
- [x] 6.2 Write a failing integration test: `PATCH /api/asistente/administracion/mantenimiento` (or similarly named endpoint), gated by `asistente.administrar`, toggles the flag; turning it on without a `razon` in the request body is rejected and the flag stays unchanged. Implement the endpoint. Verify: both the success and the missing-reason rejection pass.
- [x] 6.3 Write a failing integration test: an actor without `asistente.administrar` gets 403 from 6.2's endpoint and the flag is unchanged. Verify: test passes.
- [x] 6.4 Write a failing integration test: every toggle (on and off) writes exactly one row to the admin audit trail (group 8), naming the actor, timestamp, new state, and reason. Wire the write into the same handler as 6.2, before returning success (write-before-respond, same discipline `asistente-historial-conversaciones` used for its support-access audit). Verify: test passes, including the off-toggle case.
- [x] 6.5 Write a failing integration test: `GET /api/asistente/capacidades` reports `mantenimiento: { activo, razon }` sourced from 6.1, costing zero model calls. Wire it into `CapacidadesDto`/`ICatalogoDeCapacidades`. Verify: test passes for both states.
- [x] 6.6 Write a failing integration test: while maintenance is active, a turn from an actor without `asistente.administrar` resolves as `ServicioDegradado` with `MotivoSinModelo.Mantenimiento`'s distinct text naming the reason; an actor with `asistente.administrar` submitting a turn during maintenance is processed normally (not blocked). Wire the check into `DisponibilidadDelModeloReal.Consultar`, with the actor's permission claims checked at the call site (design.md D8: bypass is caller-side, not baked into the storage implementation). Verify: both branches pass.
- [x] 6.7 Write a failing test asserting the flag's value, once toggled, is visible identically from a second, independently opened database connection (simulating a second Host instance) without any in-process cache lingering. Verify: test passes — regression guard against accidentally caching the flag per-instance.
- [x] 6.8 Write a test asserting no Azure App Configuration package or client reference exists anywhere under `Modules.Asistente` (e.g. assert the project's package references via `dotnet list package` or a simple grep-based test). Verify: test passes, documenting the Non-Goal as an enforced regression.

## 7. Backend: remaining quota exposed to the user (A5)

- [x] 7.1 Write a failing integration test: `GET /api/asistente/capacidades` includes the requesting actor's remaining daily quota and, when blocked, the reason (`presupuesto_propio` | `tope_organizacional` | `mantenimiento`) and, when known, the reset time. Wire it using 3.2/4.3/6.1's already-built pieces (no new domain logic, just composition). Verify: test passes for the not-blocked and each blocked case.
- [x] 7.2 Write a failing integration test: a submitted turn's response includes the actor's remaining quota **as of immediately after** that turn was charged (post-decrement value, not the pre-turn value). Wire it into `ResultadoDelTurno`/`RespuestaDelAsistente.De`. Verify: test asserts the returned value is exactly one less than the value `capacidades` reported immediately before submitting.

## 8. Backend: admin-configuration audit trail

- [x] 8.1 Write a failing integration test: editing a role default, a per-user override, or the org-wide cap appends one audit row (actor, timestamp, action type, before, after) to `asistente.auditoria_administracion` (or similarly named table, part of migration 006). Wire the write into each of the budget-editing endpoints (group 9). Verify: test passes for all three edit kinds.
- [x] 8.2 Write a failing test asserting no endpoint in the module can update or delete an existing audit row (enumerate the module's routes and assert none match an update/delete shape against this table — mirroring `asistente-historial-conversaciones`'s route-enumeration test for its own audit trail). Verify: test passes.
- [x] 8.3 Add `RetencionDeAuditoriaDeAdministracionDias` to `OpcionesAsistente` (design.md D11: default 365, mirroring `RetencionDeAuditoriaDeSoporteDias`). Extend `PurgaDeRegistros` with a fifth sweep using this setting and this table's own timestamp column, following the exact pattern of its existing four `DELETE`s. Write a failing test asserting a row older than the window is purged and a row inside it survives. Verify: test passes.
- [x] 8.4 Write a failing test asserting the new audit table stays unreadable by `asistente_ro`/`asistente_ro_pii` (companion to 2.3, specific to this table). Verify: test passes with no manifest change.

## 9. Backend: usage dashboard and budget-configuration endpoints (A1, A2 admin surface)

- [x] 9.1 Write a failing integration test: `GET /api/asistente/administracion/uso` (or similarly named), gated by `asistente.administrar`, accepts a period parameter (day/week/month or explicit range) and returns per-user, per-role, and org-wide aggregates (turn count, outcome breakdown, model calls, tokens in/out/cache, avg/p95 latency, provider/model) sourced only from `asistente.registro_operativo`. Implement the aggregation query/service. Verify: test passes; a companion test asserts the response never contains a field sourced from `registro_analitico`.
- [x] 9.2 Write a failing integration test: the per-user aggregate includes each user's display name resolved via `IConsultasIdentity.ListarUsuariosAsync()` (design.md D12), and an administrator holding only `asistente.administrar` (explicitly not `usuarios.ver`) still sees full names. Verify: test passes with a test actor that has `asistente.administrar` and no other permission.
- [x] 9.3 Write a failing integration test: an actor without `asistente.administrar` gets 403 from 9.1's endpoint. Verify: test passes.
- [x] 9.4 Write a failing integration test: the dashboard's estimated cost per user/role/org uses `CalculadoraDeCosto` (group 4) and labels the value as an estimate in the response shape (e.g. a boolean/string field, not just documentation) alongside an "unpriced" bucket for unmatched provider/model rows. Verify: test passes.
- [x] 9.5 Write a failing integration test: `PUT /api/asistente/administracion/presupuestos/roles/{rol}` and `PUT /api/asistente/administracion/presupuestos/usuarios/{actorId}` (or similarly named), both gated by `asistente.administrar`, edit the role default and per-user override respectively, and both are audited (group 8). Verify: tests pass for both endpoints, including the 403 case for a caller without the permission.
- [x] 9.6 Write a failing integration test: `PUT /api/asistente/administracion/tope-organizacional` edits the org-wide monthly cap, gated the same way and audited the same way. Verify: test passes.
- [x] 9.7 Write a failing integration test: the dashboard/threshold logic emits exactly one structured (Serilog) warning the moment a user or the org first crosses 50%, 80%, or 100% within the current period, and does not repeat the same threshold's warning again within that period. Verify: test inspects emitted log events (test sink), asserting exactly one event per threshold crossing.

## 10. Documentation (AGENTS.md rule 6 — same diff as the code that changes each contract)

- [x] 10.1 Update `docs/architecture/api-contracts.md`: add the new `GET /api/asistente/administracion/uso`, `PUT /api/asistente/administracion/presupuestos/roles/{rol}`, `PUT /api/asistente/administracion/presupuestos/usuarios/{actorId}`, `PUT /api/asistente/administracion/tope-organizacional`, `PATCH /api/asistente/administracion/mantenimiento` endpoints (method, path, permission, request/response shape, error cases), and the new `remaining quota`/`mantenimiento` fields on `GET /api/asistente/capacidades` and on the turn response.
- [x] 10.2 Update `docs/architecture/data-model.md`: the six new `asistente`-schema tables (§ "El schema `asistente`"), their columns, and the inherited schema-level deny (no new `GRANT`) — same style as the existing three tables' entries.
- [x] 10.3 Update `docs/architecture/domains/asistente.md`: extend `## Presupuesto y degradación` with the new budget/org-cap/concurrency/maintenance sections (mirroring "### La cuota por actor"'s existing style), and add the new `MotivoSinModelo` values to whatever section documents the degraded outcome's causes today.
- [x] 10.4 Update `backend/src/Modules.Asistente/README.md`: extend "Endpoints (superficie HTTP)" with the five new endpoints, "Presupuesto, degradación y registros" → "Configuración" with the new `OpcionesAsistente` settings, and "Schema PostgreSQL" with the six new tables.
- [x] 10.5 Add `BR-`asistente`-007` (persistent per-user daily quota with role defaults and per-user overrides, and the charge-on-every-outcome rule) and `BR-`asistente`-008` (org-wide monthly cost cap, blocking all users regardless of individual quota) to `docs/business-rules/asistente.md`, each with a test mapping to the tasks above, following the file's existing `### BR-`asistente`-NNN <título>` format exactly.
- [x] 10.6 Update `docs/quality/tech-debt.md`'s TD-011 entry with an appended, dated note per design.md D10 (do not delete or rewrite the original text) recording that this change supersedes the in-memory acceptance for the reason stated in the proposal, and that the circuit-breaker persistence question TD-011 also raised remains open (Non-Goal of this change). Add a new TD-024 entry if this change surfaces any residual risk beyond what design.md's Risks section already states verbatim (e.g. the price-table staleness risk) — otherwise skip, per the convention `asistente-feedback-export-seguimiento` set for this same task.
- [x] 10.7 Update `docs/product/designs/asistente-conversacional-design-spec.md`: add a new section for the maintenance banner, the remaining-quota indicator in the status strip, and the gated admin dashboard screen — following the same section style used for the support-history screen precedent.

## 11. Frontend: admin dashboard screen (A1)

- [x] 11.1 Write a failing Vitest test for a new route `frontend/src/features/asistente/routes.tsx` entry (e.g. `administracion`), gated by `<RequirePermission permission="asistente.administrar">` exactly like `soporte-historial`'s existing gate — no route match at all without the permission. Implement. Verify: test passes for both the gated and ungated cases.
- [x] 11.2 Add the corresponding `nav.ts` entry with `permiso: "asistente.administrar"`, mirroring the existing `soporte-historial` entry's shape. Write a failing test asserting the nav item is absent for an actor without the permission. Verify: test passes.
- [x] 11.3 Write a failing test for a new dashboard page component: renders per-user, per-role, and org-wide totals for a selectable period, calling the endpoint from 9.1. Implement, with a period selector control. Verify: test passes; an empty-data state renders without error.
- [x] 11.4 Write a failing test: cost figures render with an explicit "estimated" label and a distinct rendering for the "unpriced" bucket, never silently shown as zero. Verify: test passes.
- [x] 11.5 Write a failing test: budget-editing controls (role default, per-user override, org cap) call the endpoints from 9.5/9.6 and show a confirmation before applying a change that lowers a currently-active quota (consistent with the app's existing pattern for destructive/impactful admin actions, e.g. the history screen's delete confirmations). Verify: test passes.
- [x] 11.6 Write a failing test: the maintenance toggle control requires entering a reason before it will submit, matching the backend's rejection rule (6.2). Implement. Verify: test passes.

## 12. Frontend: remaining quota and maintenance banner (A5, A4)

- [x] 12.1 Write a failing test: the existing status-strip component renders the remaining-quota value from `capacidades`, outside the live-region used for turn announcements. Implement. Verify: test passes.
- [x] 12.2 Write a failing test: the blocked state (quota exhausted, org cap, maintenance) is conveyed by text, not by color alone. Verify: test passes.
- [x] 12.3 Write a failing test: when maintenance is active, a visible banner renders and the message input is disabled, sourced from `capacidades.mantenimiento`. Verify: test passes.
- [x] 12.4 Write a failing test: after a turn completes, the status strip's remaining-quota value updates from that turn's own response (7.2), not from a separate re-fetch of `capacidades`. Verify: test passes.
- [x] 12.5 Write a failing test: the remaining-quota update after a turn does not move focus and is not announced inside the turn's own live-region announcement (spec: two independent UI updates). Verify: test passes.

## 13. Accessibility

- [x] 13.1 Write a failing test: every control on the admin dashboard (period selector, budget edit controls, maintenance toggle) is reachable by Tab in a logical order and operable via Enter/Space. Verify: test passes.
- [x] 13.2 Write a failing test: a saved budget edit and a completed maintenance toggle each announce via the existing live-region mechanism without moving focus unexpectedly. Verify: test passes for both flows.
- [x] 13.3 Write a failing test: the maintenance banner and the remaining-quota indicator are both reachable/readable by assistive technology without requiring the user to hover or focus a specific element first (i.e. not `title`-attribute-only). Verify: test passes.

## 14. Final verification

- [x] 14.1 Run the full backend suite and confirm green: `dotnet test backend/ArsDocendi.slnx`.
- [x] 14.2 Run the full frontend suite and confirm green: `nvm use 22 && pnpm --filter frontend test:run`.
- [x] 14.3 Run frontend lint and build: `nvm use 22 && pnpm --filter frontend lint` and `nvm use 22 && pnpm --filter frontend build`.
- [x] 14.4 Run formatting check: `pnpm format:check`.
- [x] 14.5 Validate this change strictly: `pnpm exec openspec validate asistente-administracion-de-uso --strict`.
- [x] 14.6 Re-run the `origin/develop` collision check one more time (task 1.5) immediately before opening the PR.
- [ ] 14.7 Record in the PR description any command from this list the environment could not run to completion, and why.
