# Cola de trabajo — refactor del Asistente

30 renglones. **Ruta**: `fix-bug` (red-green, invariante #9) · `directo` (PR sin change) ·
`opsx` (change primero, invariante #5) · `parar` (decisión del equipo).
**Precondición** = el comando que dice si todavía falta. Todos desde la raíz del repo.
Prompt de arranque de cualquiera: `/refactor-asistente <ID>`.

## Espejo en Linear

Épica **[ARS-102](https://linear.app/ars-docendi-chatbot/issue/ARS-102)** — proyecto «Asistente conversacional», equipo ARS.

| Renglón | Linear                  | Renglón | Linear  | Renglón | Linear  |
| ------- | ----------------------- | ------- | ------- | ------- | ------- |
| A1      | ARS-103                 | C3      | ARS-112 | E3      | ARS-122 |
| A2      | ARS-104                 | C4      | ARS-113 | E4      | ARS-123 |
| A3      | ARS-105                 | C5      | ARS-114 | E5      | ARS-124 |
| A4      | ARS-106                 | D1      | ARS-115 | F1      | ARS-125 |
| A5      | ARS-107                 | D2      | ARS-116 | F2      | ARS-126 |
| B1      | ARS-108                 | D3      | ARS-117 | F3      | ARS-127 |
| B2      | ARS-109                 | D4      | ARS-118 | F4      | ARS-128 |
| **B3**  | **ARS-85** (ya existía) | D5      | ARS-119 | G1      | ARS-129 |
| C1      | ARS-110                 | E1      | ARS-120 | G2      | ARS-130 |
| C2      | ARS-111                 | E2      | ARS-121 | G3      | ARS-131 |

**B3 no se creó**: ya existe como [ARS-85](https://linear.app/ars-docendi-chatbot/issue/ARS-85) («Escribir el invariante #14 en CLAUDE.md y cerrar el gate 0.1»), con la etiqueta _Requiere aprobación del equipo_. Se enlazó a la épica en vez de duplicarlo.

Un renglón nuevo se agrega a esta tabla **y** a Linear bajo ARS-102. Si divergen, manda el repo: la precondición es la fuente de verdad del estado, no el estado del issue.

---

### Bloque A — Seguridad (primero; sus tests rojos no esperan a nada)

| ID     | Qué                                                                                                                                                                                                                                                                                                                 | Ruta        | Precondición                                                                                                                                                                                      | Trampa                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **A1** | Decodificar `U&"…"` en `Application/TokenizadorSql.cs`. Un identificador con escapes Unicode se emite verbatim (solo a minúsculas) y llega deformado a la lista negra: hoy pasa el validador un `set_config` escrito así, y una consulta puede fijarse **otro actor**.                                              | **fix-bug** | `grep -n "case 'e' or 'E'" …/TokenizadorSql.cs` → `:71`, único prefijo contemplado                                                                                                                | `[Theory]` roja con las **tres** formas de escape verificadas contra PG 18 (`\XXXX`, `\+XXXXXX`, `UESCAPE 'c'`) **más un caso legítimo que NO debe rechazarse**. El único lookahead que existe es `Siguiente(sql,posicion)` (`:101`), de un carácter: hace falta uno de dos. `ComienzosAdmitidos` (`ValidadorDeSql.cs:111`) es lista **blanca** y no tiene este modo de falla: no la toques. El tokenizador **se queda** — reemplazarlo por un AST es una decisión de proyecto con dependencia de terceros, no un fix.                                                                                                                                                                                                                                                                               |
| **A2** | Denegar el catálogo del motor en `Application/ValidadorDeSql.cs`: prefijo `pg_` + `information_schema`, chequeado en palabras **y** en identificadores entrecomillados (los dos brazos de `MotivoDeRechazo`, `:168-183`).                                                                                           | **fix-bug** | `grep -rn "pg_catalog\|information_schema" backend/src/Modules.Asistente/Application/` → vacío                                                                                                    | **No deniegues por esquema**: `pg_catalog` está implícito en la ruta de búsqueda y `FROM pg_class` sin calificar pasaría igual. El conjunto cerrado es el de los **nombres de relación**, que empiezan todos con `pg_`. Ninguna tabla ni columna del manifiesto empieza con `pg_`: sin falsos positivos. `public` **no** entra (`public.unaccent` es la regla 8 del prefijo). No toques `CatalogoDeSensibilidad`: **necesita** leer `pg_catalog` — la regla es sobre lo que pasa por el validador. Las 12 entradas `pg_*` sueltas de `FuncionesProhibidas` quedan: el prefijo las subsume, borrarlas es ruido.                                                                                                                                                                                       |
| **A3** | Cerrar el fail-open del enmascarado: sacar `justificacion` y `tipo_baja_detalle` (`001_asistente_grants.sql:118` **y** `:119`) y `comentario` (`:122` **y** `:123`) del GRANT **a los dos roles**.                                                                                                                  | **opsx**    | `grep -c "justificacion" database/asistente/001_asistente_grants.sql` → ≥2                                                                                                                        | `:118`/`:122` son `rol_basico`; `:119`/`:123` son `rol_pii`. Con solo los pares el rol PII sigue leyendo. **Hay que tocar también `database/asistente/manifiesto-privilegios.json`** (`:268`, `:270`, `:289`, `:291`, `:332`, `:342`) y la `nota` de `:303`, que quedaría falsa — si no, `ManifiestoPrivilegiosTests` pone la suite en rojo. **Antes**: verificar contra el catálogo de preguntas si alguna respuesta las necesita. Canario obligatorio: valor único en `pedido_historial.comentario`, afirmar que no sale del enmascarador con `to_jsonb(h)`, `json_agg(h)` y `lower(comentario)` (8/8 columnas concedidas ahí). Borrar el argumento mitigante **falso** de `ClasificacionDeSensibilidad.cs:41-44` y de TD-009.                                                                     |
| **A4** | `Infrastructure/Datos/PreambuloDelActor.cs`: única declaración de `app.asistente_user_id`. Recibe conexión y transacción desde afuera, como `PrivilegiosAsistente`. Dos guards **pareados**: uno que falla si el GUC se nombra fuera, otro que verifica que toda transacción que fija el actor declara `READ ONLY`. | directo     | `grep -rn "app.asistente_user_id" backend/src/Modules.Asistente/` → 4 sitios: `EjecutorDeConsulta.cs:34`, `CatalogoDeCapacidades.cs:41`, `ConsultorDeAlcance.cs:93`, `ConsultorDeCobertura.cs:56` | El modo de falla es **silencioso**: un quinto consultor sin `set_config` hace que `identity.asistente_actor()` devuelva `NULL`, la policy dé falso, y el asistente responda _"no hay datos"_ en vez de _"no podés verlos"_ — la respuesta falsa que mide la métrica primaria del proyecto. El detector **no** puede ser `new NpgsqlConnection` (mide procedencia, no el preámbulo). Los lectores actor-independientes (`ProveedorDeEsquema`, `IndiceDeEntidades`, `CatalogoDelDominioReal`, `CatalogoDeSensibilidad`, `CatalogoDeCapacidades.LeerAsync`) **no entran**: hoy no abren transacción. Guards pareados como `ArquitecturaAsistenteTests.cs:51`/`:69`. LOC neto positivo. Red: `RlsAlcanceTests`, `EjecucionAcotadaTests`, `PermisoYPersonaPortalTests`, `Portal/RlsPortalAsistenteTests`. |
| **A5** | Cota de tiempo del lado del servidor: `ALTER ROLE … IN DATABASE … SET statement_timeout = '8s'` en `infra/scripts/provision-db.sh` (junto al `search_path` de `:77-78`), comprobado en `infra/scripts/verificar-roles-asistente.sh` contando filas en `pg_db_role_setting` (calcado de `:86-95`).                   | **opsx**    | `grep -rn "statement_timeout" database/ infra/scripts/provision-db.sh` → vacío                                                                                                                    | **No va en el DDL de `PrivilegiosAsistente`**: corre con la cadena dueña, y el dueño no puede `ALTER ROLE` de otro rol (los atributos `NOSUPERUSER NOCREATEROLE` están en `infra/scripts/_comun.sh:151`, aplicados a los dos roles de lectura). Hoy 3 de 4 conexiones de lectura corren con el default de Npgsql (30 s), el **doble** del `TimeoutDeComandoSegundos = 15` que el módulo eligió. La parte de cliente sale gratis montada sobre `AperturaDeLectura` (**E2**): no la metas acá. `provision-db.sh` necesita el ambiente y las variables de entorno: no corre sin argumentos.                                                                                                                                                                                                             |

### Bloque B — Verdad documental (riesgo nulo, paralelizable con A)

| ID     | Qué                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Ruta      | Precondición                                                                                         | Trampa                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ---------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **B1** | (a) Reescribir `## Estado` de `backend/src/Modules.Asistente/README.md:10-28`; (b) sacar `docs/product/designs/asistente-conversacional-definicion.md` de `status: draft` y enmendar su tabla de no-alcance; (c) corregir el XML-doc de `ParseoDesdeCassettesTests.cs:21-27`; (d) los tres `<summary>` huérfanos de `ModuleExtensions.cs:323-338`; (e) los dos `<see cref="Esfuerzo"/>` rotos de `OpcionesAsistente.cs:249` y `:320`; (f) mover `HuellaDelFixture` (`OpcionesAsistente.cs:410`) a su archivo. | directo   | `grep -n "Carril SQL construido, sin superficie de usuario" backend/src/Modules.Asistente/README.md` | **La tabla lista CINCO épicas, no cuatro**: E4, E5, **E6**, E7, E8. Cuatro están construidas; **E6 ("carril determinista de API vía Contracts") sigue pendiente de verdad** — Contracts tiene 0 `.cs` y el carril determinista está en sombra. Borrar esa fila es escribir una mentira nueva. (c): los 120 cassettes **son** salida real desde `2d11e7f`; el XML-doc dice lo contrario. **TD-017 no se toca** — ya dice lo correcto ("fixture congelada sin recaptura programada"); el que se lo atribuye mal es el XML-doc. (d): los tres summaries pertenecen a `ArmarAnthropic`, `ValidarEsfuerzos` y `ConstruirProveedor` (`:339`). (e): los miembros reales son `EsfuerzoDeGeneracion`, `EsfuerzoDeRedaccion`, `EsfuerzoDeReescritura`. **Va antes que E1**: los `<summary>` huérfanos migran mal cuando el archivo se parte. |
| **B2** | `/opsx:archive` sobre los 19 changes de `openspec/changes/asistente-*`, uno por uno. Saca ~8.840 líneas y desbloquea que las specs nuevas escriban `MODIFIED` en vez de `ADDED`.                                                                                                                                                                                                                                                                                                                              | opsx      | `ls -d openspec/changes/asistente-* \| wc -l` → 19                                                   | **Bloqueado hasta que la rama esté mergeada** (Step 0). `/opsx:archive` **avisa pero no bloquea** con tareas abiertas ("Don't block archive on warnings"), así que hay que mirar: `asistente-fundaciones` 56/64, `asistente-lee-portal` 46/50, `asistente-arrastre-de-consulta` 21/24, `asistente-portal-por-ambito` 13/15. Uno por uno, no en lote: un conflicto de deltas se resuelve con la vista de ese change. Cerrar con `pnpm exec openspec validate --all --strict`. **Deriva de CLI**: `openspec` global es 1.5.0 y `pnpm exec openspec` es 1.4.1; el glue `/opsx:*` usa la global. Si el devDep se bumpea, `openspec update` y glue regenerado en el mismo PR (política D7). No tildes la tarea 0.1 de `asistente-fundaciones` — eso es **B3**.                                                                          |
| **B3** | Cerrar el gate del invariante #14: `openspec/changes/asistente-fundaciones/tasks.md:5`. El invariante sigue **sin acuerdo del equipo** y cinco designs posteriores lo citan como autoridad vinculante. El propio archivo dice en `:3` que "ninguna tarea posterior arranca antes de que estas dos cierren".                                                                                                                                                                                                   | **parar** | `grep -c "\[ \]" openspec/changes/asistente-fundaciones/tasks.md` → 8                                | No es código. Se presenta al equipo y se ratifica o se cancela. **No lo decide un agente.** Ojo: la tarea 10.1 ("agregar el invariante #14 a CLAUDE.md") ya está hecha en el árbol y el change está desincronizado.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |

### Bloque C — Que la suite se pueda correr

| ID     | Qué                                                                                                                                                                                                            | Ruta    | Precondición                                                                                                                             | Trampa                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ---------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **C1** | `SembrarAsync` a `ClasePostgresAislada` (`Infraestructura/PostgresFixture.cs:187`, al lado de `AbrirConexionAsync` en `:213`), cacheada en un `Lazy<Task<string>>` estático, con `CancellationToken` opcional. | directo | `grep -rln "Task SembrarAsync" backend/tests/ \| wc -l` → **24** · `grep -rln "Task EjecutarSeedAsync" backend/tests/ \| wc -l` → **11** | **35 copias**, no 23+12. El `Lazy` no recibe token para leer el archivo: el `Task` es compartido y atarlo al token del primer test lo cancelaría para los siguientes. El `CommandTimeout = 60` **no se limpia**: el seed son 429 líneas de SQL. Borrado mecánico — una falla se ve como suite roja inmediata, nunca como falso verde; si un test empieza a fallar por conteos, el seed que corría era otro, y eso es el hallazgo. Las 11 copias de `BuscarRaizRepositorio` (TD-007) entran acá; **corregí también el texto de TD-007 y el `<remarks>` de `Infraestructura/RaizRepositorio.cs:10`, que dicen "nueve"**. `Asistente/ManifiestoPrivilegios.cs:48` **no** es una copia: busca otro archivo con otro predicado. −395 LOC.                       |
| **C2** | Carril rápido: `[Trait("carril","base")]` sobre `ClasePostgresAislada`. xUnit hereda traits de la base y las 45 clases derivan sin excepción, así que **heredar es la marca** y no puede desincronizarse.      | directo | `grep -rn "\[Trait" backend/tests/ \| wc -l` → 0 · `find . -name 'xunit*.json' \| wc -l` → 0                                             | El filtro se usa en `!=`, que es **fail-safe**: si el trait desapareciera, corre todo; con `carril=base` un typo correría cero tests en verde. **G1**: esto **no** es precedente para filtrar el evaluador. El paso extra en `ci.yml` es opcional y su valor discutible; si lo agregás va **antes** del step existente y **nunca en lugar de** él (`ci.yml:101` corre la suite completa sin filtro y sigue siendo el gate). La corrida rápida reporta **≈628 casos** (470 `[Fact]` + 38 `[Theory]` con 158 `[InlineData]` fuera de las clases con base), no 469 — 469 es el conteo de _atributos_. No agregues `[Trait]` a clases sueltas: una clase con base que no herede de `ClasePostgresAislada` es un hallazgo, no algo a parchear con una etiqueta. |
| **C3** | `EscalarAsync<T>` / `EjecutarAsync` / `Preparar` a la clase base, con una sola firma cada uno que soporte parámetros.                                                                                          | directo | `grep -rnE "async Task<T> EscalarAsync<T>" backend/tests/ \| wc -l` → 10                                                                 | Son **10 / 12 / 2**, no 7+2, y **no existe ningún `LeerAsync`**. Al menos 4 de los 12 `EjecutarAsync` **no son helpers de SQL crudo** (`EnmascaramientoDelTurnoTests.cs:324` y `EjecucionAcotadaTests.cs:414` devuelven `ResultadoDeConsulta`; `ManejadorExcepcionesApiTests.cs:57` devuelve `RespuestaProblema`; `PostgresFixture.cs:155` ya está en la infraestructura): no los toques. El `CommandTimeout = 60` pasa a **parámetro explícito con default**, no a olvido silencioso — el compilador no lo verifica, revisalo a mano. Depende de **C1**. −115 LOC.                                                                                                                                                                                        |
| **C4** | Fábrica única del grafo de `CarrilSql` en `Infraestructura/BancoDelAsistente.cs`, recibiendo `conTecho`, `contador` y `ejecutor` ya construidos.                                                               | directo | `grep -rn "new CarrilSql(" backend/tests/ \| wc -l` → ≥4                                                                                 | Hoy armado a mano en 4 sitios con **3 fechas de referencia distintas**. Homogeneizar a `2026-08-25` y **dejar** la de marzo del evaluador, que es deliberada y apunta a `GeneradorDeFixture.Ancla`. −32 LOC.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **C5** | Separar los tests puros de `ManifiestoPrivilegiosTests` y `PrefijoDeEsquemaTests` a clases sin fixture. El patrón ya existe en `CoberturaDelPortalTests`: copiarlo, no inventar otro.                          | directo | `grep -n "class ManifiestoPrivilegiosTests" backend/tests/**/ManifiestoPrivilegiosTests.cs`                                              | El compilador verifica el movimiento. 11 tests dejan de provisionar una base entera para asertar sobre texto.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |

### Bloque D — Frontera y capas

| ID     | Qué                                                                                                                                                                                                                                                                         | Ruta                | Precondición                                                                                                                                              | Trampa                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **D1** | Excepciones tipadas (`ConsultaSinPrivilegio`, `ConsultaRechazadaPorElMotor`) en `Application/CarrilSql/`, hermanas de `FallasDelProveedor`. Saca `using Npgsql` de `Application/CarrilSql.cs:2`.                                                                            | directo             | `grep -n "using Npgsql" backend/src/Modules.Asistente/Application/CarrilSql.cs` → `:2`                                                                    | Envolver `EjecutorDeConsulta` **y** `ConsultorDeAlcance`, o vuelve el 500 crudo. Reescribir `EjecucionAcotadaTests.cs:188` — ahí la aserción es `ThrowsAsync<PostgresException>` sobre una inyección de DML, **no** hay ningún `42501` en ese archivo. Las aserciones de SQLSTATE 42501 viven en `PrivilegiosLecturaTests.cs:22,31,48,70`, que usan conexión cruda y **no** las afecta este renglón.                                                                                                                                                                                                 |
| **D2** | Ruta A del Contracts: `[assembly: InternalsVisibleTo("ArsDocendi.Evaluacion.Nucleo")]` en `Properties/AssemblyInfo.cs`, junto al que ya existe para `IntegrationTests`, y `Application/` a `internal sealed` (46 de 47 archivos son `public`).                              | **parar** → directo | `grep -c "InternalsVisibleTo" backend/src/Modules.Asistente/Properties/AssemblyInfo.cs` → 1                                                               | La Ruta B (llenar Contracts) obliga a decidir antes si publicar el puerto de ejecución de SQL generado es compatible con el invariante #14; si no lo es, no cierra. **Esa pregunta no está escrita en ningún lado del repo** — la de `arquitectura-manifiesto-de-edges/design.md:137` es otra ("¿la arista `Evaluacion.Nucleo → Modules.Asistente` se conserva o se corta?"). O sea: la decisión de equipo hay que **plantearla**, no buscarla. **G2**: la arista sigue registrada como excepción con ticket. Depende de **E1** (mover primero, cambiar visibilidad después, o el diff es ilegible). |
| **D3** | `Configuracion/ValidadorDeOpcionesAsistente.cs` (`IValidateOptions`) con tres baldes: sin regla (los 4 apagados), estrictamente positivas (17), no negativas (1); más las dos relaciones (`EsperaMaximaMs ≥ EsperaBaseMs`, `TimeoutDeComando × 1000 > TimeoutDeSentencia`). | **opsx**            | `grep -rnE "IValidateOptions\|ValidateOnStart\|ValidateDataAnnotations" backend/src` → 0 (usá `-E`: con ugrep el `\|` sin `-E` es literal y da 0 siempre) | Los cuatro apagados deliberados **no llevan regla**. **Decidir `ValidateOnStart` sí/no explícitamente en el PR y escribir el motivo**: con `sí`, un `Asistente:TopeDeFilas` en cero tumba también `/api/tareas/ping`, invirtiendo la postura registrada en `ModuleExtensions.cs:76-78`. No partas `OpcionesAsistente` en varias clases: el binding no cambia. Depende de **B1** (`HuellaDelFixture` ya afuera). +70 LOC.                                                                                                                                                                             |
| **D4** | Disposición del proveedor: adaptador crudo bajo clave de servicio propia, `Dispose()` idempotente, test que afirme disposición **exactamente una vez** en las dos variantes.                                                                                                | directo             | `grep -n "ProveedorBase\|BaseDeRedaccion" backend/src/Modules.Asistente/ModuleExtensions.cs` → `:98`, `:109-110`                                          | Los records marcadores (`ProveedorConTechoDeLlamadas.cs:51` y `:61`) cortan el rastreo del DI y el `Dispose()` de `ProveedorAnthropic` (`:28`, `:200`) es inalcanzable. **Conservar `envolverProveedor`** en entrada aparte: sin eso el evaluador pierde el `MedidorDeConsumo` (`eval/ArsDocendi.Evaluacion/Program.cs:127-128`) y el eje social se auto-rechaza en silencio. Con `ModeloDeRedaccion` vacío, `:109` reusa la **misma** instancia: una disposición ingenua desde los dos records sería doble.                                                                                         |
| **D5** | `Infrastructure/Datos/ValorPerezoso<T>` y `ValorPerezosoPorRol<T>`. **Un PR por consumidor**, con los tests de `Lecturas` como red.                                                                                                                                         | directo             | `grep -rn "SemaphoreSlim" backend/src/Modules.Asistente/Infrastructure/ \| wc -l` → ≥5                                                                    | Absorbe `CacheDeCapacidades` entera y arregla una lectura concurrente **sin candado** sobre `Dictionary` (`CatalogoDeCapacidades.cs:224-254`: `TryGetValue` fuera del semáforo mientras otro hilo escribe). Documentar el cambio semántico del contador: de "veces que se consultó la base" a "veces que hubo que calcular". −55 LOC.                                                                                                                                                                                                                                                                |

### Bloque E — Reorganización semántica (el movimiento grande, sin cambio de comportamiento)

| ID     | Qué                                                                                                                                                                                                                                                                   | Ruta    | Precondición                                                                                                          | Trampa                                                                                                                                                                                                                                                                                                          |
| ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | --------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **E1** | Subcarpetas de `Application/`: `Turno/`, `CarrilSql/`, `Conversacion/`, `Abstencion/`, `Sensibilidad/`, `Determinista/`, `Modelo/`, `Lexico/`. El árbol destino está en §7 del informe.                                                                               | directo | `ls backend/src/Modules.Asistente/Application/*.cs \| wc -l` → 47                                                     | Movimiento **puro** de archivos: el namespace no cambia (mismo assembly) y el compilador verifica todo. Es el renglón que hace que la carpeta plana deje de leerse como una pila. **No es un change de OpenSpec**: un `git mv` que el compilador valida no cambia lo que el sistema promete. Depende de **B1**. |
| **E2** | `Infrastructure/Datos/AperturaDeLectura.cs`: un `NpgsqlDataSource` singleton por rol, con `CommandTimeout` decidido y no heredado. **Migrar un consumidor por PR**, con el guard de `new NpgsqlConnection` apretándose a medida que la lista de exclusiones se vacía. | directo | `grep -rn "new NpgsqlConnection" backend/src/Modules.Asistente/ \| wc -l` → 12                                        | LOC neto ≈ 0. El valor es que 12 puntos de construcción y 3 ternarios de rol pasan a 2, no la cuenta de líneas. Depende de **A4** y **A5**.                                                                                                                                                                     |
| **E3** | `ConsultorDeAlcance` de 9 round-trips a 3 para resolver 6 booleanos.                                                                                                                                                                                                  | directo | `grep -c "ExecuteScalarAsync\|ExecuteReaderAsync" backend/src/Modules.Asistente/Infrastructure/ConsultorDeAlcance.cs` | **Precondición dura: test rojo-verde de `CodigoDeRol` con dos roles vigentes distintos** — hoy ninguna aserción cubre ese valor. Depende de **E2**. −18 LOC.                                                                                                                                                    |
| **E4** | Partir `Application/CapaConversacional.cs` (551 líneas contra el cap soft de ~300 de `golden-principles.md:49`): `FabricasDelResultado.cs` y `CierreDelTurno.cs` (`TextoDelRechazo` + `VinculosAsync`) a archivos hermanos. Baja a ~350.                              | directo | `wc -l backend/src/Modules.Asistente/Application/CapaConversacional.cs` → 551                                         | Son **4** fábricas privadas que devuelven `ResultadoDelTurno` (`NecesitaAclaracion` :456, `Caido` :496, `Degradado` :515, `SinDatos` :535), no 5 — `ResolverAclaracion` (:414) devuelve una tupla. **No lo conviertas en pipeline de middlewares**: ver `modulo.md` §5.1. Depende de **E1**.                    |
| **E5** | Mover el prompt de generación de `RenderizadorDeEsquema` a `Infrastructure/Catalogo/InstruccionesDeGeneracion.cs`, junto a su único consumidor.                                                                                                                       | directo | `grep -c "regla" backend/src/Modules.Asistente/Infrastructure/RenderizadorDeEsquema.cs`                               | **Toca la huella del prefijo.** Verificá los cassettes de generación antes de mergear: `ce1ede8` ya cambió `RenderizadorDeEsquema.cs` sin tocarlos y dejó 56 cassettes como peso muerto. Depende de **E1** y, si existe, de **G3**.                                                                             |

### Bloque F — Frontend (paralelizable con todo)

| ID     | Qué                                                                                                                                                                                                                          | Ruta    | Precondición                                                                                                     | Trampa                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ---------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **F1** | `import "../asistente.css"` en `components/LanzadorAsistente.tsx` + helper `test/hojas.ts` que barra el **directorio** para el guard de tokens. Borrar `.adoc-asistente-inicio-entrada` (`asistente.css:154`, regla muerta). | directo | `grep -rn "asistente.css" frontend/src/features/asistente/ --include='*.tsx'` → solo `pages/AsistentePage.tsx:7` | Hoy el modal de la barra se estiliza **por accidente**; un `lazy()` sobre `/asistente` lo deja pelado. **NO borres `.adoc-asistente-quien` ni la reemplaces por `.adoc-sr`**: `asistente.css:277-287` incluye `user-select: none` y el comentario de `:270-276` documenta el bug exacto que evita (alguien copió «Vos» pegado y la pregunta que llegó al modelo fue «Vosdame 3 materias…»); `.adoc-sr` de la librería usa `clip` **sin** `user-select`. Además la clase está viva en `Mensaje.tsx:29`/`:63` y `Mensaje.test.tsx:237` la afirma. **Tres** archivos leen `asistente.css` por nombre: `asistente.tokens.test.ts:14`, `RutaConAltoPropio.test.tsx:14`, `TablaDeResultado.test.tsx:15`. −17 LOC. |
| **F2** | `frontend/src/features/asistente/index.ts` como API pública (`routes` + `LanzadorAsistente`) + regla `no-restricted-imports` en `src/app/**`.                                                                                | directo | `grep -n "features/asistente/components" frontend/src/app/shell/TopBar.tsx` → `:8`                               | Es el **único** import profundo cross-frontera de todo el frontend: las otras **ocho** features pasan por `<feature>/routes`. La regla lo convierte de convención tácita en error de build.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **F3** | `frontend/src/shared/hooks/useDescartarAlClicAfuera.ts` (el directorio no existe todavía) consumido por los 4 popovers duplicados en 3 capas.                                                                                | directo | `ls frontend/src/shared/hooks/ 2>/dev/null` → no existe                                                          | Test de las **dos** ramas: la del clic afuera no está probada en ningún lado del repo. −33 LOC.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **F4** | **Crear** un guard de clases CSS huérfanas, sobre el texto sin comentarios.                                                                                                                                                  | directo | `grep -rn "huerfan" frontend/src/features/asistente/*.test.ts` → solo el de **tokens**                           | El guard **no existe**: hay que escribirlo, no arreglarlo. El de tokens (`asistente.tokens.test.ts:52`) es sobre `var(--…)` y **ya** corre sobre `sinComentarios` (definido en `:30`). Depende de **F1**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |

### Bloque G — Rendimiento de la suite (el más riesgoso, al final)

| ID     | Qué                                                                                                                                                                                                                        | Ruta    | Precondición                                                                                                                  | Trampa                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ----------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **G1** | Plantilla de PostgreSQL: `CREATE DATABASE … TEMPLATE`. Hoy 534 casos, cada uno con `CREATE DATABASE` + 3 `MigrateAsync` + 2 `CREATE ROLE` + privilegios + registros + `DROP DATABASE WITH (FORCE)`.                        | directo | `grep -c "TEMPLATE" backend/tests/ArsDocendi.IntegrationTests/Infraestructura/PostgresFixture.cs` → 0                         | **Obligatorio reaplicar `ALTER ROLE … IN DATABASE "<clon>" SET search_path = ''`** — vive en `pg_db_role_setting`, cuya clave es el OID de la base, y **no se clona**; sin eso los tests que sostienen el invariante #14 pasan a **falso verde**. Test que falle si el `search_path` efectivo sobre un clon no es vacío. Migrar **clase por clase**, solo donde el 100% de los casos siembra (6 clases): en las 18 restantes hay ~117 tests que arrancan con base vacía a propósito. **G3** del Step 5: el orden identity → designaciones → portal no cambia. Depende de **C1**. |
| **G2** | `AssemblyFixture`: sacar el `[Collection]` compartido de las 45 clases (uno por archivo, no en la clase base), que hoy serializa 488 tests sin necesidad — el aislamiento real ya lo da el nombre único de base por clase. | directo | `grep -rc "CollectionDefinition" backend/tests/ArsDocendi.IntegrationTests/Infraestructura/PostgresFixture.cs` → 1 (en `:13`) | Arrancar con `maxParallelThreads` 2-4 y **medir contra la línea de base**; vigilar el DDL concurrente sobre `pg_authid` y que `ClearAllPools()` es process-wide. Depende de **G1**.                                                                                                                                                                                                                                                                                                                                                                                              |
| **G3** | Guard del prefijo del esquema en los cassettes: clase propia con base, contra el fixture del evaluador, excluyendo las tres huellas de instrucciones recalculándolas.                                                      | directo | `grep -rn "PrefijoDeEsquema" backend/tests/**/Cassette*.cs` → sin guard de prefijo                                            | El sello tiene dos mitades y solo la del fixture tiene barrido en CI. **Ya se degradó una vez en esta rama**: `ce1ede8` cambió `RenderizadorDeEsquema.cs` sin tocar cassettes, la suite quedó verde sobre un corpus irreproducible, y recuperarlo costó una corrida financiada contra el proveedor real. Depende de **C1**. +65 LOC.                                                                                                                                                                                                                                             |

**Renglón nuevo**: se agrega acá con los mismos campos —incluida su precondición— en el bloque
que corresponda. Este archivo es la única cola; no abrir un doc de plan aparte.

---

## Qué NO tocar

Imperativo. Ningún renglón de la cola pisa nada de esto.

1. **No metas `backend/eval/ArsDocendi.Evaluacion` en la solución.** Está fuera del `.slnx` a
   propósito, con seis guards adentro, porque el criterio es qué cuesta dinero. El `[Trait]`
   de **C2** no es precedente para filtrarlo.
2. **No muevas `backend/src/ArsDocendi.Evaluacion.Nucleo/`.** `LectorDeAristas.LeerBackendSrc()`
   está cableado a ese árbol: sacarlo convierte la única fila de excepción de
   `backend/manifiesto-de-aristas.json` en "fila sin arista" y rompe el test del invariante #2.
3. **No reordenes la migración del fixture.** Es identity → designaciones → portal
   (`PostgresFixture.cs:78, 93, 98`): la RLS de portal referencia `designaciones.designaciones`.
4. **No sumes Polly ni `AddStandardResilienceHandler()`.** Reintroduce reintento **y** breaker
   en el pipeline HTTP: la cota de 12 requests por turno pasa a 36, con un segundo corte que
   `DisponibilidadDelModeloReal` no puede observar.
5. **No borres comentarios ni XML-docs para bajar el conteo.** En `Application/` el ratio
   comentario:código es 1,02:1 y esos comentarios citan el bug real que cada decisión evita.
   Son el activo más valioso del módulo.
6. **No conviertas `CapaConversacional` en un motor de etapas.** El `ContextoDelTurno` mutable
   que ese patrón exige ya está rechazado por escrito en `README.md:586-589`, y los pasos ya
   tienen sus tests en aislamiento. Lo que queda en `ResolverAsync` es composición: se parte en
   archivos hermanos (**E4**), no se le monta un pipeline.
7. **No hagas extract-method sobre `AddAsistenteModule`.** Siete archivos de test ejercitan un
   área sola contra la registración de producción **entera**, a propósito
   (`CadenasDeConexionTests.cs:14-18`).
8. **No pongas `[Range(1, …)]` sobre las perillas.** Cuatro usan el cero como apagado
   deliberado y documentado: `CupoDeLlamadasPorActor`, `FallosParaAbrirElBreaker`,
   `PresupuestoDelTurnoSegundos`, `TopeDeTurnosDelHistorial`.
9. **No borres el carril determinista en sombra** (838 líneas). Está declarado, argumentado y
   es reversible: es un compromiso con ARS-46, y se revierte por decisión del equipo.
10. **No muevas `NormalizadorLexico` a `ArsDocendi.Shared`.** No es utilidad pura transversal:
    lleva el diccionario de sinónimos del dominio del asistente. Lo consume exactamente un módulo.
11. **En A3, no toques el GRANT sin tocar `manifiesto-privilegios.json`.** El manifiesto es lo
    que hace falsable al invariante #14; desincronizarlo pone la suite en rojo y, peor, deja el
    argumento escrito diciendo algo falso.
12. **En F1, no reemplaces `.adoc-asistente-quien` por `.adoc-sr`.** Esa clase tiene
    `user-select: none` contra un bug real y documentado; la de la librería no lo tiene.
13. **No corras comandos de build ni de test desde `backend/`.** Desde ahí el pin del SDK
    (`10.0.201` contra `10.0.111` instalado) hace fallar todo. Desde la raíz funciona.
14. **No agregues un guard nuevo sin su par sintético.** La convención de
    `ArquitecturaAsistenteTests` es alimentar cada detector con una violación fabricada, para
    que una regex rota no pase en verde para siempre.
15. **No cuentes líneas como métrica de éxito.** El refactor completo mueve el 1%.
